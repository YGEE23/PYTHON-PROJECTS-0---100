pwd = input ("What is the master password")

mode = input("Would you like ot add a new password or view existing ones (view/add), press q to quit. ").lower()
if mode == "q":
    break

if mode == "view":
    pass
elif mode == "add":
    pass
else:
    print("Invalid Mode. ")
    continue